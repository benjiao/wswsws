FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN yarn install --frozen-lockfile

# Copy source code
COPY . .

EXPOSE 3000

# Use development mode with hot reload
CMD ["yarn", "dev"]
