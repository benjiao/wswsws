# Admin Interface
I used unfold to style the Admin UI: https://unfoldadmin.com/docs/installation/quickstart/

# Commands

### Tasks
```
docker compose exec api python manage.py generate_treatment_instances <treatment_schedule_id>
```
