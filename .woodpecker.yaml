steps:
  - name: Push Trigger Test
    image: alpine:latest
    commands:
      - echo "=== Push Trigger Test ==="
      - echo "Push triggered pipeline"
      - echo "Commit: ${CI_COMMIT_SHA}"
      - echo "Branch: ${CI_COMMIT_BRANCH}"
      - echo "Event: ${CI_BUILD_EVENT}"
    when:
      - event: push
      - branch: main
